numpy
scipy
pennylane
matplotlib
networkx

# Development:
pytest

# Optional GPU statevector backend (for the large N=256 case):
#   NVIDIA / CUDA:  pennylane-lightning-gpu      (device: lightning.gpu)
#   AMD / ROCm:     pennylane-lightning-amdgpu   (device: lightning.amdgpu, MI300 + ROCm>=7.0)
